"use server";
//import { redirect } from "next/navigation";

export async function createInvoice(formData: FormData) {
  console.log(formData);
  // Create a new invoice
  // ...

  // Redirect to the new invoice
  //redirect(`/invoice/${data.id}`);
}
